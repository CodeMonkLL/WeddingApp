*.db         # Ignoriert alle SQLite-Datenbanken
Data/quiz.db        # ignoriert die spezifische SQLite-Datenbank
/bin/        # Ignoriert Build-Ordner (Standard für .NET)
/obj/        # Ignoriert ebenfalls Build-Ordner